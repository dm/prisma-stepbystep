generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Movie {
  director     String
  id           Int    @default(autoincrement()) @id
  movieName    String
  yearReleased Int
}